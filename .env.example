# EmailJS credentials — get these from https://dashboard.emailjs.com
# Copy this file to `.env` and fill in your values. Without them, the
# contact form gracefully falls back to opening the visitor's mail client.
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
